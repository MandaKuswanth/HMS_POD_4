import React from"react";import{ActivityIndicator,Text,TouchableOpacity,StyleSheet}from"react-native";import COLORS from"../utils/colors";export default function AppButton({title,onPress,disabled,loading,color=COLORS.primary,textColor="#fff",style}){return <TouchableOpacity style={[s.btn,{backgroundColor:color},(disabled||loading)&&s.disabled,style]} onPress={onPress} disabled={disabled||loading} activeOpacity={.85}>{loading?<ActivityIndicator color={textColor}/>:<Text style={[s.text,{color:textColor}]}>{title}</Text>}</TouchableOpacity>}const s=StyleSheet.create({btn:{borderRadius:50,paddingVertical:14,alignItems:"center",justifyContent:"center"},disabled:{opacity:.65},text:{fontSize:15,fontWeight:"900"}});
